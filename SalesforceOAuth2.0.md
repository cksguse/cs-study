# [CS] OAuth 2.0 기반 시스템 연동 및 인증 방식 정리

## 1. OAuth 2.0의 본질 (인증 vs 인가)
* **인증(Authentication)** : "당신이 누구인가?"를 확인하는 신원 절차 (ID/PW 확인)
* **인가(Authorization)** : "당신이 어떤 자격에 접근할 권한이 있는가?"를 확인하고 권한을 위임하는 절차
* **OAuth 2.0의 목적** : 사용자의 비밀번호를 외부 어플리케이션에 직접 노출하지 않고, 제한된 자원에 접근할 수 있는 **액세스 토큰(Access Token)**을 발급받기 위한 표준 인가 프레임워크.

---

## 2. 클라이언트 자격 증명 흐름 (OAuth 2.0 Client Credentials Flow)
사용자의 개입(Interactive Login) 없이 **서버 대 서버(Server to Server / M2M)**가 직접 통신할 때 사용하는 가장 단순한 인증 방식.
* **동작 방식**
1. 클라이언트(서버)가 사전에 발급받은 **Client ID(Consumer Key)**와 **Client Secret(Consumer Secret)**을 인가 서버(세일즈포스)에 직접 전송
2. 인가 서버가 두 자격 증명을 검증한 뒤, API 호출용 `access_token`을 즉시 반환
* **특징 및 한계**
  * 구현이 매우 단순하고 직관적이다
  * `Client Secret` 문자열이 네트워크를 통해 전송되므로, Secret 유출 시 보안 위험이 존재한다
  * 백엔드 배치(Batch), 데몬 프로세스, 내부 시스템 간 데이터 동기화에 주로 사용된다.

---

## 3. JWT 베어러 토큰 흐름 (OAuth 2.0 JWT Bearer Flow)
비밀번호나 Secret을 직접 전송하지 않고, **비대칭 암호화(공개키/개인키 전자서명)**을 통해 보안성을 극대화한 자동화 서버 연동 방식

* **JWT(Jason Web Token) 구조**
  * **Header** : 알고리즘 방식(`RS256` 등) 및 토큰 타입 정의
  * **PayLoad (Claims)** : 발급자, 사용자 식별자, 대상 서버, 만료 시간 등의 데이터
  * **Signature (전자 서명)** : `Header + Payload`를 클라이언트의 **개인키(Private Key)**로 암호화한 서명값

* **동작 방식**
1. 세일즈포스 외부 클라이언트 앱(External Client App)에 **디지털 인증서(공개키)**를 등록하고 사용자 권한을 사전승인한다.
2. 클라이언트가 개인키로 서명한 JWT를 생성하여 인가 서버로 전송
3. 세일즈포스는 등록된 공개키로 서명의 진위 여부와 만료 시간을 검증한 후 `access_token`을 발급
* **특징**
  * Secret이 네트워크를 타지 않으므로 탈취 위험이 없다
  * 엔터프라이즈 CI/CD, 자동화 배치 및 백엔드 연동의 최신 표준이다
