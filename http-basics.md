# HTTP（HyperText Transfer Protocol）の基本まとめ

## 1. HTTPとは？
クライアント（ブラウザやアプリなど）とサーバー間でデータをやり取りするための**Webアプリケーション層（Application Layer）のプロトコル**。
---
## 2. HTTPの主な特徴
**ステートレス（無状態性）**
- サーバーがクライアントの以前の状態を記憶しない特徴。
- サーバーの拡張性が向上するが、状態保持のために**クッキー（Cookie）**、**セッション**、**JWTトークン**などを活用する。
**コネクションレス（ノンコネクション）**
- リクエストに対してレスポンスを返した後、接続を切断する方式。
- サーバーリソースの無駄を削減できるが、接続時のオーバーヘッドを防ぐため、HTTP/1.1以降では**Keep-Alive**による接続の再利用がデフォルトでサポートされている。
---
## 3. 主要なHTTPメソッド（HTTP Methods）
- **Get** : サーバーからのリソース取得をリクエスト（読み取り専用）
- **Post** : サーバーへの新しいリソース作成をリクエスト（Bodyデータを含む）
- **Put** : 指定したリソースの全体置換・更新
- **Patch** : 指定したリソースの一部更新
- **Delete** : 指定したリソースの削除
---
## 4. リクエストとレスポンス（Request&Response）
HTTP通信は、クライアントからの**リクエスト**と、サーバーからの**レスポンス**のペアで成り立っている。
### ① リクエストの構造
クライアントがサーバーへ送信するメッセージ。
1. **Start Line（リクエストライン）** : HTTPメソッド、URL、HTTPバージョン（例：`Get/users HTTP/1.1`）
2. **Headers（リクエストヘッダー）** : ホスト名、認証情報、データ形式などのメタデータ
3. **Body（リクエストボディ）** : POSTやPUTなどでサーバーへ送信する実際のデータ（JSONなど）
### ② レスポンスの構造
サーバーが処理結果をクライアントへ返却するメッセージ。
1. **Status Line（ステータスライン）** : HTTPバージョン、ステータスコード、テキスト（例：`HTTP/1.1 200 OK`）
2. **Headers（レスポンスヘッダー）** : レスポンスのデータ形式、Cookie設定（Set-Cookie）、サーバー情報など
3. **Body（レスポンスボディ）** : クライアントに返却するHTMLやJSONなどの実際のデータ
---
## 5. HTTPとWeb API（REST API）の関係
HTTPは**データを送信するための通信規約（プロトコル）**であり、APIは**クライアントとサーバーがやり取りするためのインターフェース**。
現代のWeb APIは、HTTPの構成要素を積極的に活用して構築されている。
