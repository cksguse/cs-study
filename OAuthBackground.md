# OAuth 인증 방식은 왜 등장했는가

## 1. OAuth 이전의 문제: 비밀번호 공유 방식

OAuth가 나오기 전, 서드파티 앱이 다른 서비스의 데이터에 접근하려면 사용자의 **아이디/비밀번호를 직접 입력받아 저장**하는 방식이 일반적이었다.

예를 들어 어떤 사진 인화 서비스가 "당신의 구글 포토 사진을 인화해드립니다"라는 기능을 제공한다면, 사용자는 그 서비스에 자신의 구글 계정 비밀번호를 그대로 입력해야 했다. 이 방식을 흔히 **"password anti-pattern"**이라고 부른다.

이 방식에는 다음과 같은 심각한 문제가 있었다.

- **비밀번호 노출 위험**: 서드파티 서비스가 비밀번호를 평문 또는 암호화된 형태로 저장해야 하므로, 그 서비스가 해킹당하면 원본 서비스(구글, 트위터 등)의 계정까지 뚫린다.
- **전체 권한 위임**: 비밀번호를 알면 그 계정으로 할 수 있는 모든 행동(메일 읽기, 삭제, 설정 변경 등)이 가능해진다. "사진만 읽어가고 싶다"는 요청인데도 계정 전체 권한을 넘겨줄 수밖에 없었다. 즉 **세분화된 권한 제어(scope)가 불가능**했다.
- **철회의 어려움**: 서드파티 접근을 끊고 싶으면 원래 계정의 비밀번호 자체를 바꿔야 했다. 그러면 그 비밀번호를 쓰던 다른 모든 서드파티 연동도 함께 끊어진다. 특정 앱 하나만 골라서 접근을 취소할 방법이 없었다.
- **신뢰의 무한 확장**: 사용자가 이용하는 서드파티 서비스가 늘어날수록, 원본 계정 비밀번호를 알고 있는 곳이 계속 늘어난다. 신뢰해야 하는 대상이 통제 불가능하게 커진다.

## 2. 요구사항: "비밀번호 없이, 제한된 권한만, 철회 가능하게"

이 문제들을 해결하려면 다음 세 가지가 필요했다.

1. 사용자가 비밀번호를 서드파티에게 절대 알려주지 않아야 한다.
2. 서드파티에게 필요한 만큼만(예: 읽기 전용, 특정 데이터만) 권한을 줄 수 있어야 한다.
3. 특정 서드파티에 대한 접근 권한만 개별적으로 취소할 수 있어야 한다.

## 3. OAuth의 등장

이런 배경에서 2007년경 트위터, 구글 등 여러 서비스 개발자들이 모여 공통 표준을 만들기 시작했고, 이것이 **OAuth**(Open Authorization)로 이어졌다. 이후 OAuth 1.0(2010, RFC 5849)을 거쳐, 모바일/웹 환경에 맞게 단순화된 **OAuth 2.0**(2012, RFC 6749)이 현재 표준으로 자리잡았다.

OAuth의 핵심 아이디어는 **"인증(Authentication)과 권한 부여(Authorization)를 분리"**하는 것이다.

- 사용자는 비밀번호를 오직 신뢰하는 원본 서비스(예: 구글의 로그인 화면)에만 입력한다.
- 서드파티는 비밀번호 대신 **제한된 권한을 가진 토큰(Access Token)**을 발급받는다.
- 이 토큰은 특정 범위(scope)의 작업만 허용하며, 유효기간이 있고, 언제든 원본 서비스에서 개별적으로 철회할 수 있다.

## 4. 정리

| 구분 | Before (비밀번호 공유) | After (OAuth) |
|---|---|---|
| 비밀번호 노출 | 서드파티에 그대로 전달 | 절대 전달하지 않음 |
| 권한 범위 | 전체 권한(all-or-nothing) | scope로 세분화 가능 |
| 철회 | 비밀번호 변경 시 전체 연동 해제 | 서드파티별 개별 철회 가능 |
| 신뢰 구조 | 비밀번호를 아는 곳이 계속 증가 | 토큰 발급자(원본 서비스)만 신뢰 |

결국 OAuth는 "내 계정을 통째로 넘기지 않고도, 필요한 서비스에게 필요한 만큼의 권한만 안전하게 빌려주기 위해" 등장한 인증/인가 프로토콜이라고 요약할 수 있다.
