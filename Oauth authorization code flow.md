# OAuth 2.0 Authorization Code Flow 정리

> Salesforce를 예시로 한 OAuth 2.0 인가 코드 흐름(Authorization Code Flow) 학습 정리

## 목차

- [1. 콜백 URL과 리다이렉트의 의미](#1-콜백-url과-리다이렉트의-의미)
- [2. 인가 코드 → 액세스 토큰 교환 과정](#2-인가-코드--액세스-토큰-교환-과정)
- [3. 콜백 URL이 필요한 흐름 vs 필요 없는 흐름](#3-콜백-url이-필요한-흐름-vs-필요-없는-흐름)
- [4. 인가 코드 발급 조건과 Client Secret의 역할](#4-인가-코드-발급-조건과-client-secret의-역할)
- [5. Client ID/Secret 불일치 시 동작](#5-client-idsecret-불일치-시-동작)
- [6. 인가 코드 vs 토큰 교환, 어느 쪽이 더 안전한가](#6-인가-코드-vs-토큰-교환-어느-쪽이-더-안전한가)
- [7. 브라우저와 서버의 차이](#7-브라우저와-서버의-차이)

---

## 1. 콜백 URL과 리다이렉트의 의미

콜백 URL로 "리다이렉트"된다는 것은 사용자 브라우저 주소창이 해당 URL로 이동한다는 뜻(HTTP 302 Redirect)이지만, **콜백 URL은 사람이 보라고 만든 화면이 아니라 서버의 엔드포인트**다.

- 브라우저가 콜백 URL로 이동하면, 그 URL을 처리하는 서버(OAuth 클라이언트 앱의 백엔드)가 요청을 가로채서 쿼리 파라미터에 담긴 인가 코드(`code`)를 즉시 추출한다.
- 사용자 눈에는 잠깐 로딩 후 "로그인 성공" 같은 최종 화면으로 넘어가는 정도만 보이고, 콜백 URL 자체의 내용을 직접 보는 경우는 거의 없다.

## 2. 인가 코드 → 액세스 토큰 교환 과정

1. 브라우저가 콜백 URL로 리다이렉트되며 **인가 코드(authorization code)** 전달
2. 콜백 URL을 받는 서버(예: Salesforce 백엔드)가 이 코드를 수신
3. 서버가 **사용자 개입 없이** 자체적으로 외부 IdP(인증 서버)의 토큰 엔드포인트에 코드를 재전송 (서버 대 서버 통신)
4. 토큰 엔드포인트가 코드를 검증하고 **access token + refresh token** 발급
5. 서버가 토큰을 저장, 이후 API 호출에 사용

> 2~4단계는 사용자가 보지 못하는 **백채널(back-channel) 통신**이다. 사용자는 1단계에서 로그인/동의(consent)만 하면 그 이후는 전부 자동으로 처리된다.

## 3. 콜백 URL이 필요한 흐름 vs 필요 없는 흐름

콜백 URL(리다이렉트 URI)이 필요한 것은 **브라우저를 거쳐야 하는 흐름**뿐이다.

| 흐름(Flow) | 콜백 URL 필요 여부 | 특징 |
|---|---|---|
| Authorization Code Flow (Web Server Flow) | ✅ 필요 | 브라우저로 로그인 → 리다이렉트 → 코드 전달 |
| Implicit Flow | ✅ 필요 | 리다이렉트 시 URL에 토큰이 바로 포함 (구식, 비권장) |
| Client Credentials Flow | ❌ 불필요 | 사용자 없이 서버 대 서버로 client_id/secret만으로 토큰 발급 |
| JWT Bearer Flow | ❌ 불필요 | 서명된 JWT로 사용자 상호작용 없이 토큰 발급 |
| Username-Password Flow | ❌ 불필요 | ID/PW를 직접 담아 요청 (브라우저 개입 없음, 보안상 비권장) |

즉, **사용자에게 로그인 화면을 보여주고 동의를 받아야 하는 방식**에서만 콜백 URL이 필요하고, 서버끼리 직접 통신하는 방식에서는 콜백 URL 자체가 존재하지 않는다.

## 4. 인가 코드 발급 조건과 Client Secret의 역할

**인가 코드 발급 시점(1단계)** 과 **client secret 검증 시점(2단계)** 은 분리되어 있다.

### 인가 코드가 발급되는 조건 (1단계)

- **사용자의 로그인 정보(ID/PW)** — 사용자 본인 확인
- **사용자의 동의(consent)** — 앱의 정보 접근 허용 여부
- **client_id** — 어떤 앱이 요청하는지 식별 (URL 쿼리 파라미터에 평문 노출됨)

이 단계에서 **client secret은 전송되지 않는다.** 즉 인가 코드는 secret 없이도 발급된다.

### client secret이 쓰이는 시점 (2단계)

- Salesforce 서버가 인가 코드를 들고 토큰 엔드포인트에 요청할 때, `code`와 함께 `client_id` + `client_secret`을 함께 전송
- 이때 비로소 인가 서버가 "이 요청을 보낸 서버가 정말 등록된 그 앱이 맞는지"를 secret으로 검증

### 왜 이렇게 분리되어 있는가

- 1단계는 **브라우저(공개 채널)** 를 거치므로, 여기에 secret을 실으면 노출 위험이 크다. 그래서 "짧은 시간만 유효한 일회용 코드"만 발급한다.
- 2단계는 **서버 대 서버(비공개 채널, HTTPS)** 이므로 secret을 안전하게 전송할 수 있다.
- 결과적으로 공격자가 인가 코드를 가로채더라도, secret이 없으면 코드를 토큰으로 교환할 수 없는 **이중 안전장치** 구조가 된다.

## 5. Client ID/Secret 불일치 시 동작

1. 사용자 로그인 + 동의 → 인가 서버가 **인가 코드**를 콜백 URL로 전달 (client_id만으로 진행, secret 불필요)
2. 세일즈포스 서버가 코드를 들고 토큰 엔드포인트에 요청하며 **client_id + client_secret** 함께 전송
3. 인가 서버가 사전에 등록된 secret과 대조
   - ✅ **일치** → access token + refresh token 발급
   - ❌ **불일치** → 요청 거부, 토큰 미발급 → 결과적으로 API 접근 불가능

secret은 "이 요청을 보낸 게 정말 등록된 애플리케이션(Salesforce 서버)이 맞다"는 것을 증명하는 열쇠 역할을 하며, 이게 없으면 인가 코드를 손에 넣어도 무용지물이 되는 것이 핵심 보안 설계다.

## 6. 인가 코드 vs 토큰 교환, 어느 쪽이 더 안전한가

인가 코드가 URL 노출/가로채기 위험이 있다면, secret을 동반한 토큰 교환 구간도 가로채일 가능성이 있는가에 대한 정리.

### 두 통신 구간의 노출 위험 비교

| 구분 | 인가 코드 전달 (1단계) | 토큰 교환 (2단계) |
|---|---|---|
| 통신 주체 | 브라우저 ↔ 인가 서버 | 서버 ↔ 서버 (직접 연결) |
| 노출 경로 | URL 쿼리 파라미터 → 브라우저 히스토리, 서버 로그, Referer 헤더, 프록시 로그 등에 남을 수 있음 | HTTPS(TLS)로 암호화된 채널, 중간 경유 컴포넌트가 적음 |
| 가로채기 난이도 | 상대적으로 쉬움 (브라우저 확장, 악성 프록시, 로그 유출 등) | 훨씬 어려움 (서버 인프라 자체를 침투해야 함) |

- 인가 코드는 여러 경로를 거칠 수 있는 브라우저 환경을 지나가기 때문에, **일회용 + 짧은 유효시간**(보통 몇 분 이내)으로 설계되어 가로채여도 빨리 무효화된다.
- 토큰 교환은 HTTPS로 암호화된 서버 간 직접 통신이라 중간자가 끼어들 여지가 훨씬 적다.

### 서버 간 통신도 가로채일 수 있는가

이론적으로는 가능하다 (TLS 설정 취약, 서버 자체 해킹, 내부망 MITM 등). 다만 이는 OAuth 프로토콜 설계의 결함이라기보다 **일반적인 서버/인프라 보안 문제**의 영역이다.

### 참고: PKCE (Proof Key for Code Exchange)

모바일 앱, SPA처럼 secret을 안전하게 저장할 수 없는 **공개 클라이언트(public client)** 를 위한 확장.

- secret 대신 매 요청마다 랜덤 생성한 `code_verifier` / `code_challenge` 쌍을 사용
- 인가 코드가 가로채이더라도, 원래 요청을 보낸 클라이언트가 아니면 토큰 교환이 불가능
- Salesforce처럼 secret을 서버에 안전하게 보관할 수 있는 **기밀 클라이언트(confidential client)** 는 보통 secret 방식을 사용하고, PKCE는 그럴 수 없는 환경의 보완책으로 사용됨

## 7. 브라우저와 서버의 차이

"서버가 프론트엔드를 표시하는 것"이라기보다는, **역할과 위치가 다른 두 개의 프로세스**로 이해하는 것이 정확하다.

| 구분 | 브라우저 | 서버 |
|---|---|---|
| 위치 | 사용자의 컴퓨터/스마트폰 안에서 실행 | 서비스 제공자(예: Salesforce)가 관리하는 별도의 컴퓨터에서 실행 |
| 통제 주체 | 사용자가 직접 조작 (주소창, 클릭 등) | 개발사가 통제, 사용자는 직접 접근 불가 |
| 역할 | 화면을 보여주고 사용자 입력을 서버에 전달 | 요청 처리, DB 조회, 외부 API 호출, secret 등 민감정보 보관 |

- 프론트엔드는 "브라우저에서 실행되는 화면/코드", 백엔드(서버)는 "눈에 안 보이는 곳에서 로직을 처리하는 부분"을 가리킨다.
- 정확히는 서버가 만든 화면(HTML)을 브라우저가 받아서 렌더링하는 관계다.

### OAuth 흐름에 대입

```
[사용자] ↔ [브라우저] ↔ (인터넷) ↔ [인가 서버 / 로그인 페이지]
                                         ↑
                              로그인 화면(프론트엔드)을
                              서버가 만들어 브라우저에 전송
```

- **1단계 (인가 코드 발급)**: 브라우저가 인가 서버 접속 → 인가 서버가 로그인 화면(HTML)을 만들어 브라우저에 전송 → 브라우저가 화면을 렌더링 → 사용자 입력 → 인가 서버로 전달 → 인가 서버가 콜백 URL로 리다이렉트 지시
- **2단계 (토큰 교환)**: **브라우저가 아예 등장하지 않는다.** 백엔드 서버가 인가 서버의 API 엔드포인트에 직접 HTTP 요청을 보내고 응답(토큰)을 받는, 순수한 서버 대 서버 통신이다.

> "브라우저를 거친다"는 것은 사람 눈에 보이고 사람이 조작할 수 있는 환경을 거친다는 뜻이고, "서버 대 서버"는 사람도 화면도 없이 기계끼리 바로 통신한다는 뜻이다.
