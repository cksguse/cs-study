# 세일즈포스 동시성 제어 및 비동기 레코드 갱신 안티패턴 (Salesforce Concurrency & Asynchronous Update Anti-Patterns)

---

## 1. 동시성 제어 및 충돌 감지 (Concurrency & Collision Detection)

### 1.1 에러 발생 메커니즘
```text
[유저 브라우저]   화면 로드 (버전 1 스냅샷) ──────────────▶ 항목 편집 후 [저장] 클릭 (충돌 에러)
                                                              ▲
                                                              │ (타임스탬프 불일치 감지)
[백그라운드 비동기] Connect API 통신 ──▶ DB 레코드 갱신 완료 (버전 2로 갱신)
```
1. **LDS 캐시 스냅샷**: 유저가 화면을 열 때 브라우저의 라이트닝 데이터 서비스(Lightning Data Service / LDS)는 당시의 타임스탬프(`SystemModstamp` / `LastModifiedDate`)를 로컬 메모리에 보관합니다.
2. **백그라운드 갱신**: 비동기 프로세스(Asynchronous Path / 非同期パス)가 서버 DB 레코드를 `update`하여 서버 타임스탬프가 변경됩니다.
3. **낙관적 동시성 제어(Optimistic Concurrency Control)**: 유저가 저장을 누르면 서버는 브라우저가 보낸 타임스탬프와 DB의 타임스탬프를 비교하여, 편집 도중 레코드가 변경되었음을 감지하고 데이터 유실 방지를 위해 **충돌 감지(Collision Detection / 競合検出)** 에러를 발생시킵니다.

### 1.2 시간 경과와 새로고침의 관계
* 충돌 감지는 타임아웃 방식이 아니라 **버전 대조 방식**입니다.
* 따라서 1분이 지나든 1시간이 지나든, 유저가 **웹 페이지 새로고침(Reload / 再読み込み / F5)**을 통해 LDS 캐시를 최신화하지 않는 한 무조건 충돌 에러가 발생합니다.

### 1.3 지연 로딩(Lazy Loading)과 탭 라이프사이클
* `lightning-tabset`은 비활성 탭의 컴포넌트를 미리 렌더링하지 않는 **지연 로딩(Lazy Loading / 遅延読み込み)** 방식을 취합니다.
* **정상 케이스**: 채팅 탭만 보다가 비동기 처리가 끝난 후 상세 탭을 처음 클릭하면, 컴포넌트가 처음 마운트되며 최신 DB 데이터를 조회합니다.
* **에러 발생 케이스**: 비동기 처리가 끝나기 전에 상세 탭을 열었거나, 사전에 상세 탭을 열어두어 브라우저 캐시에 과거 버전이 남아있는 상태에서 편집·저장할 경우 충돌이 발생합니다.

---

## 2. Connect API와 동일 레코드 비동기 갱신 안티패턴

### 2.1 Connect REST API가 콜아웃(Callout)으로 분류되는 이유
1. **아키텍처 계층 분리**: 코어 트랜잭션 엔진과 Connect API 서비스 계층(마이크로서비스)이 물리적/논리적으로 분리되어 있어 내부 루프백(Loopback HTTP) 통신을 거칩니다.
2. **즉시 커밋 특성 (Non-Rollbackable)**: Connect API 작업(Chatter, 알림 등)은 트랜잭션 롤백이 불가능한 부수 효과(Side Effect)를 가지므로 DML 트랜잭션과 직접 결합할 수 없습니다.

### 2.2 아키텍처 패턴 비교

| 패턴 구분 | 설계 형태 | 리스크 및 평가 |
|---|---|---|
| **안티패턴 (Anti-Pattern)** | 유저 화면 인터랙션 레코드($Record)를 **비동기 패스에서 동일 레코드로 다시 `update`** |  유저 수정 작업과 100% 충돌 감지(Collision Detection) 발생 |
| **안전한 비동기 패턴** | 외부 시스템 단방향 전송, 알림 발송, 무관한 로그 레코드 생성 | 화면 레코드 버전과 무관하여 충돌 없음 |
| **정석 아키텍처 대안 1** | **수동 실행 버튼 (Screen Flow / Quick Action / LWC)** | 유저가 원할 때 명시적 실행 후 화면 즉시 자동 갱신 |
| **정석 아키텍처 대안 2** | **야간 스케줄 배치 (Batch Apex / Scheduled Flow)** | 실시간이 불필요한 경우 야간 일괄 처리로 주간 경합 원천 차단 |
